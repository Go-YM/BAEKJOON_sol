#include <stdio.h>

int main(void)
{
	int arr[9] = {0,};
	int max=0;
	int maxindex;

	for(int i=0;i<9;i++)
	{
		scanf("%d",&arr[i]);
	}

	for(int i=0;i<9;i++)
	{
		if(max<arr[i])
		{
			maxindex=i;
			max = arr[i];
		}
	}

	printf("%d\n",max);
	printf("%d",maxindex+1);

	return 0;
}
